===================== Instruction to Run the code =====================
1) use Google colab to run the code 
2) click on Edit->Notebook Settings and select GPU
3) click Connect 
4) After connecting to google server upload the dataset file (i.e. crowd_transliterations.hi-en.txt)
5) Run the code.